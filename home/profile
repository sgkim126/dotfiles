if [ "$PS1" ]; then
    if [ "$BASH" ] && [ "$BASH" != "/bin/sh" ]; then
        # The file bash.bashrc already sets the default PS1.
        # PS1='\h:\w\$ '
        if [ -f /etc/bash.bashrc ]; then
            source /etc/bash.bashrc
        fi
        # include .bashrc if it exists
        if [ -f "$HOME/.bashrc" ]; then
            source "$HOME/.bashrc"
        fi
    elif [ "`id -u`" -eq 0 ]; then
        PS1='# '
    else
        PS1='$ '
    fi
fi

if [ -f "$HOME/.bash_completion" ]
then
    source "$HOME/.bash_completion"
fi

if [ -d "$HOME/.rvm/bin" ] ; then
    export PATH="${PATH}:$HOME/.rvm/bin"
fi

if [ -d "$HOME/.root" ] ; then
    export PREFIX="$HOME/.root"
    if [ -d "$PREFIX/bin" ] ; then
        export PATH="$PREFIX/bin:$PATH"
    fi
    if [ -d "$PREFIX/lib" ] ; then
        export LD_LIBRARY_PATH="$PREFIX/lib:$LD_LIBRARY_PATH"
    fi

    if [ -d "$PREFIX/opt/node/lib/node_modules" ]
    then
        export NODE_PATH="$PREFIX/opt/node/lib/node_modules:$NODE_PATH"
    fi

    if [ -d "$PREFIX/opt/ant" ]
    then
        export ANT_HOME="$PREFIX/opt/ant"
    fi

    if [ -d "$PREFIX/opt/scala" ]
    then
        export SCALA_HOME="$PREFIX/opt/scala"
        export PATH="$SCALA_HOME/bin:$PATH"
    fi
fi

if [ -d "/usr/lib/jvm/java-7-openjdk-amd64" ] ; then
    export JAVA_HOME="/usr/lib/jvm/java-7-openjdk-amd64"
fi

LINUXBREW_PREFIX="$HOME/.linuxbrew"
if [ -d "$LINUXBREW_PREFIX/bin" ]
then
    export PATH="$LINUXBREW_PREFIX/bin:$PATH"
fi
if [ -d "$LINUXBREW_PREFIX/share/man" ]
then
    export MANPATH="$LINUXBREW_PREFIX/share/man:$MANPATH"
fi
if [ -d "$LINUXBREW_PREFIX/share/info" ]
then
    export INFOPATH="$LINUXBREW_PREFIX/share/info:$INFOPATH"
fi
unset LINUXBREW_PREFIX

# vim:set ft=sh:
